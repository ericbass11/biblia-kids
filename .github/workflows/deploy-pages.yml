name: Deploy no GitHub Pages

# Publica o app (site estático) no GitHub Pages.
on:
  push:
    branches:
      - main
      - master
      - claude/app-historias-biblicas-catolicas-254cc5
  workflow_dispatch:

# Permissões necessárias para o deploy no Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Garante um único deploy por vez (não cancela um deploy em andamento)
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Pages
        uses: actions/configure-pages@v5

      - name: Enviar arquivos do site
        uses: actions/upload-pages-artifact@v3
        with:
          # O app é estático e mora na raiz do repositório
          path: "."

      - name: Publicar no GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
